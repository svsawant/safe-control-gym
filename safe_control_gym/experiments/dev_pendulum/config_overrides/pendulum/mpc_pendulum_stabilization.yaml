algo: mpc
algo_config:
  horizon: 50
  r_mpc:
    - 0.01
  q_mpc:
    - 1
    - 0.5
  # Prior info
  prior_info:
    # prior_prop: 
    #   pole_length: 1.0
    #   pole_mass: 1.0
    prior_prop: null
    randomize_prior_prop: False
    prior_prop_rand_info: null
  warmstart: True
  solver: ipopt
  # solver: qrsqp

  
